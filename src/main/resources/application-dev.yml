spring:
  thymeleaf:
    cache: false
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/board_game?characterEncoding=UTF-8
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}